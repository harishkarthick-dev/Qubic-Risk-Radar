# NOTE: These individual Dockerfiles are kept for reference
# The main unified Dockerfile at the root now builds both frontend and backend
# To use the old separate container setup, modify docker-compose.yml to use:
#   context: ./backend
# instead of:
#   context: .
